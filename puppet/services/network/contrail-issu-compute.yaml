heat_template_version: 2016-10-14

description: >
  Contrail Controller ISSU

parameters:
  ServiceNetMap:
    default: {}
    description: Mapping of service_name -> network name. Typically set
                 via parameter_defaults in the resource registry.  This
                 mapping overrides those in ServiceNetMapDefaults.
    type: json
  DefaultPasswords:
    default: {}
    type: json
  EndpointMap:
    default: {}
    description: Mapping of service endpoint -> protocol. Typically set
                 via parameter_defaults in the resource registry.
    type: json
  ContrailControlASN:
    description: Autonomous System Number
    type: number
    default: 64512
  NeutronMetadataProxySharedSecret:
    description: Metadata Secret
    type: string
    hidden: true

resources:
  ContrailBase:
    type: ./contrail-base.yaml
    properties:
      ServiceNetMap: {get_param: ServiceNetMap}
      DefaultPasswords: {get_param: DefaultPasswords}
      EndpointMap: {get_param: EndpointMap}

outputs:
  role_data:
    description: Role Contrail Controller ISSU
    value:
      service_name: contrail_issu
      config_settings:
        map_merge:
          - get_attr: [ContrailBase, role_data, config_settings]
          - contrail::issu::host_ip: {get_param: [ServiceNetMap, ContrailISSUNetwork]}
            contrail::control::asn: {get_param: ContrailControlASN }
            contrail::vrouter::metadata_proxy_shared_secret: {get_param: NeutronMetadataProxySharedSecret}
      step_config: |
        include ::tripleo::network::contrail::issu_compute
